<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Airlock</RootNamespace>
    <AssemblyName>Airlock.Core</AssemblyName>
  </PropertyGroup>

  <!-- Local project reference while Exec()/ExecAsync() are unreleased; swap back to the
       PackageReference once a CShell with them ships. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\CShell\src\CShell.csproj" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Assets\**\*" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Airlock.Core.Tests" />
  </ItemGroup>

</Project>
